#pragma once

class MarkdownEncoderSettings
{
  public:
    MarkdownEncoderSettings();
    MarkdownEncoderSettings(const MarkdownEncoderSettings& object);
    virtual ~MarkdownEncoderSettings();

    MarkdownEncoderSettings& operator=(const MarkdownEncoderSettings& other);

};
